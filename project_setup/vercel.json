{
    "version": 1.0,
    "builds": [
      {
        "src": "project_setup/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "25mb", "runtime": "python3.10" }
      },
      {
        "src": "build_files.sh",
        "use": "@vercel/static-build",
        "config": {
          "distDir": "staticfiles_build"
        }
      }
    ],
    "routes": [
      {
        "src": "/static/(.*)",
        "dest": "/static/$1"
      },
      {
        "src": "/(.*)",
        "dest": "project_setup/wsgi.py"
      }
    ]
  }